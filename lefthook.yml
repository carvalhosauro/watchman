pre-commit:
  parallel: true
  commands:
    fmt:
      run: test -z "$(gofmt -l .)"
    lint:
      run: golangci-lint run
    test:
      run: go test ./...
commit-msg:
  commands:
    conventional:
      run: bash scripts/commit-msg.sh {1}

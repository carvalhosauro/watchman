#!/usr/bin/env bash
# Configure git to use the project hooks directory (.githooks/).
set -e

git config core.hooksPath .githooks
chmod +x .githooks/*

echo "✓ Git hooks installed (.githooks/)"
echo "  pre-commit: gofmt staged Go files + golangci-lint + go test"
echo "  commit-msg: Conventional Commits check"

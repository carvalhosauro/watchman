#!/usr/bin/env bash
# Configure git to use project hooks directory

set -e

git config core.hooksPath .githooks
chmod +x .githooks/*

echo "✓ Git hooks installed (.githooks/)"
echo "  Pre-commit: auto-formats staged .ex/.exs files"
